---
allowed-tools: Bash(npm run lint), Bash(npm run build)
description: Run the standard verification loop
---

Run:
1) `npm run lint`
2) `npm run build`

If any step fails:
- Diagnose root cause,
- Fix,
- Re-run verification until clean.
